function mOver(obj, clr, bgclr){/*mOver(this, '글자색', '배경색')*/
  var os = obj.style;
  os.backgroundColor = bgclr;
  os.color = clr;
}
function mOut(obj, clr, bgclr){/*mOut(this, '글자색', '배경색')*/
  var os = obj.style;
  os.backgroundColor = bgclr;
  os.color = clr;
}
